/*
Still adding to this, but I'm collecting the various date logic.
The third one will get all records back to midnight on the same day.

*/
SELECT
    DATEADD(dd,DATEDIFF(dd,0,GETDATE())-1,0) as Date1
,   DATEDIFF(dd,0,GETDATE())-1 as Date2
,   Convert(DateTime, DATEDIFF(DAY, 0, GETDATE())) as Date3
FROM _Job
